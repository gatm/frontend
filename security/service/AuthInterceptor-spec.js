describe('AuthInterceptor', function() {

  beforeEach(module('security'));

  it('should ...', inject(function(AuthInterceptor) {

	//expect(AuthInterceptor.doSomething()).toEqual('something');

  }));

});